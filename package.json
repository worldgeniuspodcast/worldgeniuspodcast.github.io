{
  "name": "worldgeniuspodcast",
  "scripts": {
    "dev": "imba app/index.html",
    "build": "imba build app/index.html",
    "deploy": "npm run build && npx touch dist/.nojekyll && echo worldgeni.us > dist/CNAME && npx gh-pages --no-history --dotfiles --dist dist/"
  },
  "dependencies": {
    "express": "^4.17.1",
    "imba": "^2.0.0-alpha.206"
  }
}
